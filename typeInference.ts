// Type Inference (declaração direta)

let message2 = 'mensagem definida';

let car = {
  penus: 'novos',
  potenciaMotor: 600
}

window.addEventListener('click', (e) => {
  console.log(e.target);
})
